angular.module('tickTockApp')
  .controller('InicioCtrl', function ($scope) {
    $scope.message = "Inicio.";
  });
 
angular.module('tickTockApp')
  .controller('AlarmaCtrl', function ($scope) {
    $scope.message = "Alarma.";
  });